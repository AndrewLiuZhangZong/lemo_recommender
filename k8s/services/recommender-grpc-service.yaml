apiVersion: v1
kind: Service
metadata:
  name: lemo-service-recommender
  namespace: lemo-dev
  labels:
    app: lemo-service-recommender
    component: grpc
    version: v1
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
  selector:
    app: lemo-service-recommender
    component: grpc

