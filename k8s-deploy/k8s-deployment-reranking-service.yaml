apiVersion: apps/v1
kind: Deployment
metadata:
  name: reranking-service
  namespace: lemo-dev
  labels:
    app: reranking-service
    version: v2.0
spec:
  replicas: 1  # 测试环境：1个副本
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: reranking-service
  template:
    metadata:
      labels:
        app: reranking-service
        version: v2.0
    spec:
      imagePullSecrets:
      - name: regcred
      containers:
      - name: reranking-service
        image: ${IMAGE}
        imagePullPolicy: IfNotPresent
        command: ["python", "services/reranking/main.py"]
        env:
        - name: HOST
          value: "0.0.0.0"
        - name: PORT
          value: "8083"
        envFrom:
        - configMapRef:
            name: lemo-service-config
        ports:
        - containerPort: 8083
          name: http
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "300m"
        livenessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 8083
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: reranking-service
  namespace: lemo-dev
  labels:
    app: reranking-service
spec:
  type: ClusterIP
  selector:
    app: reranking-service
  ports:
  - port: 8083
    targetPort: 8083
    name: http

