apiVersion: v1
kind: Service
metadata:
  name: lemo-service-recommender-vector-generation
  namespace: lemo-dev
  labels:
    app: vector-generation-service
    version: v1
spec:
  type: ClusterIP
  ports:
  - port: 8093
    targetPort: 8093
    protocol: TCP
    name: grpc
  selector:
    app: vector-generation-service

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: vector-generation-service
  namespace: lemo-dev
  labels:
    app: vector-generation-service
    version: v1
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vector-generation-service
      version: v1
  template:
    metadata:
      labels:
        app: vector-generation-service
        version: v1
    spec:
      containers:
      - name: vector-generation-service
        image: registry.cn-beijing.aliyuncs.com/lemo/recommender:latest
        imagePullPolicy: Always
        command: ["python", "services/vector-generation/main.py"]
        ports:
        - containerPort: 8093
          name: grpc
          protocol: TCP
        envFrom:
        - configMapRef:
            name: lemo-services-config
        - secretRef:
            name: lemo-services-secret
        env:
        - name: GRPC_PORT
          value: "8093"
        resources:
          requests:
            cpu: 300m
            memory: 512Mi
          limits:
            cpu: 1000m
            memory: 2Gi
      imagePullSecrets:
      - name: aliyun-docker-registry

